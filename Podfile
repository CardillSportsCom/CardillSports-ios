# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'stattracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for stattracker
  pod 'Firebase/Core'
  pod 'FirebaseUI'
  pod 'FBSDKCoreKit'
  pod 'Alamofire', '~> 5.0.0-beta.5'
  pod 'SwiftyJSON'

  target 'stattrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'stattrackerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
